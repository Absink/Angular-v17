import { ColorDaysDirective } from './color-days.directive';

describe('ColorDaysDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDaysDirective();
    expect(directive).toBeTruthy();
  });
});
